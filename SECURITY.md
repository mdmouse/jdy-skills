# 安全说明

## 这是第三方项目，不是简道云官方出品

「简道云」是**帆软软件有限公司**的注册商标。本项目与帆软无隶属、无合作、无授权关系，
不使用官方 logo，也不代表官方立场。它只是一组调用简道云**公开开放 API** 的
Agent 技能，出问题请找本项目，不要找简道云客服。

官方另有「AI 连接」（原 MCP 服务）。那是官方产品，和本项目是两回事。

## 数据流向

装上之后，代码只会把数据发往两个地方：

| 目的地 | 什么时候 | 发什么 |
|---|---|---|
| `api.jiandaoyun.com` | 每次读写你的表单 | 简道云开放 API 的请求体 |
| **你自己配的**群机器人 | 只有 `jdy-report` / `jdy-watch` / `jdy-flow-ops` 的推送功能，且你确认过 | 报表正文、命中记录、催办摘要 |

群机器人地址由你写在配置里，代码不内置任何一个。能识别的类型是企业微信
（`qyapi.weixin.qq.com`）、飞书（`open.feishu.cn` / `open.larksuite.com`）、
钉钉（`oapi.dingtalk.com`）。

**没有遥测、没有崩溃上报、没有任何回传给作者的通道。** 代码里出现的
其余 `http://` 字符串都是 XML 命名空间或注释，不是网络调用。

生成出来的文件（HTML 报告、周报 Markdown、数据字典、集成样例、修复建议表）
末尾带一行 `aicliagent` 署名。**那是写死在产物里的静态文字**——不是像素、
不是脚本、不加载任何远程资源，也不会因此多发出一个请求；HTML 报告里那个链接
只有你自己点了才会去访问。设 `JDY_BRAND=0` 可以让产物里不留这一行。
推给群机器人的消息正文从不带署名。

## 零第三方依赖

全部脚本只用 Python 标准库，`pip install` 一个包都不需要——
xlsx 读写、YAML 子集、聚合统计都是自己实现的。供应链攻击面因此是零：
你装的就是你读到的这些文件，它们不会在运行时再去拉别的东西。

（这不是洁癖：各端沙箱里有 Python 但没有 pip 通道，带依赖会把
「平台不支持」和「依赖装不上」混成同一种失败。安全性是顺带的好处。）

## 你的 API Key

- 只从环境变量 `JDY_API_KEY` 或 `~/.jdy/config.json` 读取。
  **技能目录和本仓库里零密钥**，`tests/test_no_secrets.py` 每次跑都在守这条。
- 把 `~/.jdy/config.json` 设成 `600`。
- 建 Key 时请建**最小权限**的：限定应用 ＋ 只给用得到的接口 ＋ IP 白名单。
  简道云的 Key 没有细粒度作用域时等同管理员权限，别拿全权限 Key 跑自动化。
- 推送日志里的 webhook token 会被打码后再输出（`_shared/webhook.py` 的 `mask()`）。

## 写操作的护栏

写入类技能默认不写：

- **默认 dry-run**，先出计划、你确认了才执行。
- **`JDY_WRITE_ALLOWLIST`**：设成 entry_id / app_id 列表后，任何写接口目标不在
  名单里就直接拒绝。校验做在唯一的 HTTP 出口上，绕不过去。
- **写前整表备份、写后逐字段回读核对**，静默丢弃会被报出来而不是当成成功。
- **`jdy-org` 一个删除接口都没接**（删除成员／批量删除／删除部门都没有），
  而且要另开 `JDY_ORG_WRITE=1` 才肯写——它动的是整个企业的组织架构，
  按表单限定的写入白名单对它是瞎的。
- `jdy-doc`、`jdy-query`、`jdy-devkit`、`jdy-watch` 只读。

**能被改回去的只有还存在的记录。** 备份能还原被改写的值，但任何工具都变不回
已经被删掉的记录——删除请在简道云界面上做，并且想清楚。

## 报告漏洞

寄 **hi@aicliagent.com**，标题带上 `[security]`。

请附：受影响的技能与版本、复现步骤、你认为的影响面。
**别在邮件里附上你的真实 API Key 或客户数据**——需要样本时我会另约方式。

我会在 **72 小时内**回执，并在修复发布后于 CHANGELOG 致谢（你要求匿名则不写）。
在补丁发布前请先不要公开披露。

只维护最新一个发布版本；旧版本不回补。

## 不在范围内的

- 简道云平台本身的漏洞 —— 请报给帆软官方。
- 你自己配置错的 Key 权限、写入白名单、机器人地址造成的后果。
- 各端 Agent 宿主（WorkBuddy / 千问办公 / 豆包工作等）的沙箱与权限模型。
